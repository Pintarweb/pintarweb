.hidden { display: none !important; }
body {
    font-family: 'Outfit', 'Inter', sans-serif;
    background: #f8fafc;
    color: #0f172a;
}

.folder-active {
    background: #0f172a;
    color: #fff;
}

.glass-card {
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.3);
}

.animate-pulse-green {
    animation: pulse-green 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

@keyframes pulse-green {
    0%, 100% {
        opacity: 1;
        transform: scale(1);
    }
    50% {
        opacity: 0.8;
        transform: scale(0.95);
    }
}

/* Custom Scrollbar */
::-webkit-scrollbar {
    width: 6px;
}
::-webkit-scrollbar-track {
    background: transparent;
}
::-webkit-scrollbar-thumb {
    background: #cbd5e1;
    border-radius: 10px;
}
::-webkit-scrollbar-thumb:hover {
    background: #94a3b8;
}

/* Pipeline Stage Dots */
.stage-dot {
    transition: all 0.3s ease;
}

.stage-line {
    transition: background-color 0.3s ease;
}

/* Intake Panel */
#intake-panel textarea,
#intake-panel input[type="text"],
#intake-panel select {
    background: white;
}

#intake-panel input[type="radio"]:checked + div {
    border-color: #3b82f6;
    background-color: #eff6ff;
}

/* Profiles View */
#profile-history-modal > div,
#profile-form-modal > div {
    animation: modal-slide 0.2s ease-out;
}

@keyframes modal-slide {
    from { opacity: 0; transform: scale(0.95) translateY(-10px); }
    to { opacity: 1; transform: scale(1) translateY(0); }
}

#profiles-view table button[disabled] {
    pointer-events: none;
}

.filter-row { display: grid; grid-template-columns: repeat(6, minmax(0, 1fr)); }
